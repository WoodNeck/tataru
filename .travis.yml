dist: trusty
sudo: false
branches:
  only:
    - staging
language: python
python:
  - "3.5"
  - "3.6"
before_install:
  - "pip install -U pip"
install:
  - pip install -r requirements.txt
script:
  - pytest